[project]
name = "keylog"
version = "0.1.0"
description = "物理鍵の貸出・返却をICカード(FeliCa)で管理するデスクトップ台帳アプリ"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyscard>=2.0",
    "customtkinter>=5.2",
    "reportlab>=4.0",
]

[tool.uv]
package = false
