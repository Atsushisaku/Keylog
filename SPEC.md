# 鍵管理簿アプリ「Keylog」仕様書

最終更新: 2026-07-08 / ステータス: ドラフト（レビュー待ち）

---

## 1. 目的

部屋・保管庫などの **物理鍵の貸出・返却をデジタル台帳で管理**する。
利用者の特定には FeliCa 内蔵の **IC カード**（社員証等）を使い、カードをかざすだけで
「誰が・いつ・どの鍵を借り/返したか」を記録する。

## 2. 前提・環境

| 項目 | 内容 |
|---|---|
| 稼働形態 | **単一 PC のデスクトップアプリ**（ネットワーク不要） |
| OS | Windows 11 |
| カードリーダー | SONY RC-S300（PC/SC 認識名 `SONY FeliCa Port/PaSoRi 4.0`） |
| カード種別 | FeliCa（IDm を利用者識別子に使用） |
| 言語 / GUI | Python (>=3.10) + **CustomTkinter**（今風の見た目） |
| パッケージ/仮想環境 | **uv** で管理（`pyproject.toml` + `uv.lock`、`.venv` 生成） |
| DB | SQLite（`keylog.db` 単一ファイル） |
| カード読み取り | pyscard（PC/SC 経由）※導入済み |
| PDF 生成 | **reportlab**（日本語は同梱 CID フォント `HeiseiKakuGo-W5` を使用、フォント同梱不要） |

## 3. 用語定義

| 用語 | コード名 | 定義 | 識別子 |
|---|---|---|---|
| 鍵 | `key` | 管理対象の物理キー（部屋・保管庫等） | 管理番号 `code` |
| IC カード | `ic_card` | 利用者を特定する FeliCa カード | IDm |
| 利用者 | `user` | 鍵を借りる/返す人 | 利用者ID |
| 貸出記録 | `checkout` | 誰が・いつ・どの鍵を借り/返したかの1件 | 記録ID |

※ FeliCa カードを「鍵」「カードキー」とは呼ばない（物理鍵との衝突回避）。

## 4. 確定した設計方針（ヒアリング結果）

1. **未登録カード** → かざした場所（貸出/返却画面）で **その場で登録フロー**に進む。
2. **カードと利用者は 1 対 1**（1人1枚）。
3. **権限管理は管理者 PIN のみ** — 貸出/返却は誰でも可。利用者・鍵の登録/編集/削除など
   マスタ操作時のみ PIN を要求。
4. **鍵の選択は画面の一覧からクリック**（物理鍵に FeliCa/バーコードは付けない）。

## 5. データモデル（SQLite スキーマ）

1対1（カード:利用者）のため、IC カードは独立テーブルにせず **利用者テーブルに IDm 列を持たせる**
（構造が単純・整合性事故が少ない）。

```sql
-- 利用者（IC カードは idm 列で 1:1 に保持）
CREATE TABLE users (
    user_id    INTEGER PRIMARY KEY AUTOINCREMENT,
    name       TEXT    NOT NULL,
    idm        TEXT    UNIQUE,            -- 紐づく IC カード。未割当なら NULL
    active     INTEGER NOT NULL DEFAULT 1,-- 0=無効(退職者等)。削除でなく論理削除
    created_at TEXT    NOT NULL
);

-- 鍵
CREATE TABLE keys (
    key_id     INTEGER PRIMARY KEY AUTOINCREMENT,
    code       TEXT    NOT NULL UNIQUE,   -- 管理番号（刻印等）
    name       TEXT    NOT NULL,          -- 名称（例: 会議室A）
    note       TEXT,                      -- 備考（設置場所など）
    active     INTEGER NOT NULL DEFAULT 1,-- 0=廃棄鍵。論理削除
    created_at TEXT    NOT NULL
);

-- 貸出記録（returned_at IS NULL = 貸出中）
CREATE TABLE checkouts (
    checkout_id    INTEGER PRIMARY KEY AUTOINCREMENT,
    key_id         INTEGER NOT NULL REFERENCES keys(key_id),
    user_id        INTEGER NOT NULL REFERENCES users(user_id),
    checked_out_at TEXT    NOT NULL,
    returned_at    TEXT                  -- NULL の間は貸出中
);

-- 管理者 PIN 等の設定
CREATE TABLE settings (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
-- 例: ('admin_pin_hash', '<sha256>')

-- 点検記録（管理者が数か月に一度、管理簿と実物の鍵を突合した記録）
CREATE TABLE inspections (
    inspection_id INTEGER PRIMARY KEY AUTOINCREMENT,
    inspected_at  TEXT    NOT NULL,       -- 点検日時
    inspector     TEXT    NOT NULL,       -- 点検者名
    result        TEXT    NOT NULL,       -- 'ok' / 'issue'（問題あり）
    note          TEXT,                   -- 所見・不一致の内容
    open_snapshot TEXT                    -- 点検時点の貸出中一覧のスナップショット(JSON)
);

-- 【重要】二重貸出を DB レベルで物理的に不可能にする部分ユニークインデックス
-- 1つの鍵に「未返却(returned_at IS NULL)」の記録は同時に1件までしか作れない。
CREATE UNIQUE INDEX ux_open_checkout
    ON checkouts(key_id) WHERE returned_at IS NULL;
```

**接続時の必須 PRAGMA**（SQLite 既定では OFF のため毎接続で設定）:
```sql
PRAGMA foreign_keys = ON;   -- 外部キー制約を有効化
PRAGMA journal_mode = WAL;  -- 破損耐性・オンラインバックアップ用
```

**設計上のポイント**
- **鍵の「在庫中/貸出中」状態は列で持たず、`checkouts` から導出**する
  （`returned_at IS NULL` の記録があれば貸出中）。二重管理による不整合を防ぐため。
- **二重貸出防止（二重化）**: ①上記の部分ユニークインデックスで DB が物理的に拒否（最後の砦）、
  ②アプリでも貸出前に未返却記録の有無を検査（UX 用）。制約違反は握って
  「その鍵は貸出中です」と表示。
- **1利用者は同時に複数の鍵を借用可**（記録が複数 open になるだけ）。
- **論理削除と貸出中の相互作用**:
  - 利用者・鍵は `active=0` で無効化（物理削除しない、履歴保護）。
  - **open な貸出がある鍵/利用者は無効化・廃棄できない**（先に返却を要求）。
  - **返却系・貸出状況のクエリは `active` を無視**する（無効化済みでも返却・把握が可能）。
  - 無効化した利用者の `idm` は NULL に戻し、カードを別人へ再利用可能にする。
- **退職者カードの扱い**: `active=0` かつ idm 一致のカードを翳した場合は**貸出/返却を拒否**し、
  「無効な利用者のカードです」を表示（本人になりすました貸出を防ぐ）。
- **日時はローカル時刻の naive ISO8601 文字列**（例 `2026-07-08T14:30:00`）で統一保存。
  SQLite の `datetime('now')`（=UTC）は使わず、アプリ側で `datetime.now()` を用いる
  （9時間ズレ防止）。

## 6. 画面構成

### 6.1 メイン画面（貸出/返却）— PIN 不要
- 常時カード待受（バックグラウンドでリーダーをポーリング）。
- 上部に **［貸出モード］／［返却モード］** 切替。
- **カードをかざす** と利用者を特定して下の一覧を表示:
  - **貸出モード**: `active=1` かつ在庫中の鍵一覧 → クリック → **確認ダイアログ** → 貸出記録を作成
    （誤クリック対策）。
  - **返却モード**: その利用者が借用中の鍵一覧 → クリックで返却（`returned_at` を記録）。
- **未登録カード検知時**:
  - **貸出モードのみ**「未登録カードです。登録しますか？」→ **管理者 PIN 入力** → 名前入力 →
    `users` に `name + idm` で新規登録し、そのまま貸出フローへ（登録は常に PIN 必須で §6.2 と一貫）。
  - **返却モードでは登録に進まない**（返却対象が無く無意味なため）。「未登録カードです」の表示のみ。
- **退職者(active=0)カード検知時**: 「無効な利用者のカードです」を表示し操作を拒否。
- 操作後は「○○さんが 会議室A を貸出しました」等のトースト表示 → 数秒で待受に戻る。

### 6.2 管理メニュー — 操作時に PIN 要求
- **利用者管理**: 一覧 / 追加 / 名前編集 / 無効化 / カード再登録（かざして idm 更新）。
  - カード再登録で新 idm が既存 active 利用者と衝突（UNIQUE 違反）した場合はエラー表示し中止。
- **鍵管理**: 一覧 / 追加 / 編集 / 廃棄（無効化）。※open な貸出がある鍵は廃棄不可。
- **貸出状況**: 現在貸出中の一覧（誰が・何を・いつから）＝**返し忘れの把握**。
  - **代理返却**: 借用者が不在/退職/カード再割当済みでも、管理者がここから返却できる
    （open record が回収不能になるのを防ぐ）。
  - **記録の訂正・取消**: 誤った貸出/返却記録を PIN 付きで修正・取消できる。
- **PIN 変更**: 管理者 PIN を変更できる。初期 PIN からの変更を促す。
- **履歴**: 貸出記録の一覧・期間/利用者/鍵でのフィルタ・**CSV エクスポート**（監査/棚卸し用）。
- **鍵別使用レポート（PDF）**: 鍵を選び期間を指定 → その鍵を「誰が何時〜何時に使ったか」の
  一覧を **PDF 出力**（§7 参照）。
- **点検記録**: 数か月に一度の管理簿×実物チェックの実施を記録（日時・点検者・結果・所見）。
  過去の点検履歴も一覧表示。

## 7. カード読み取りの扱い（技術）
- リーダーを **別スレッドで定期ポーリング**し、カードを検知（Tk のメインループをブロックしない）。
- 取得するのは **IDm のみ**（利用者特定に十分。PMm 等は保存しない）。
- 読み取り失敗（かざし位置ズレ等）は自動リトライ。多重検知は 1 回に集約
  （同一 IDm を離すまで再発火しない）。

### 7.1 スレッド安全性（厳守）
Tkinter もデフォルトの SQLite 接続も**スレッド非安全**。以下を設計として固定する:
- **ワーカースレッドは検知した IDm を `queue.Queue` に put するだけ**。
  UI や DB には一切触らない。
- **UI スレッドが `root.after(...)` で定期的にキューを drain** し、
  IDm→利用者照会・画面更新をすべて **UI スレッド上で**行う。
- DB 接続は UI スレッドで開く（`check_same_thread` 既定のまま）。ワーカーは DB を持たない。

### 7.2 単一起動ガード（必須）
- RC-S300 の PC/SC 接続は**排他的**で、2 起動目はリーダー取得に失敗する。加えて多重起動は
  DB 競合の温床になる。
- 起動時に **named mutex / ロックファイル**で二重起動を検出し、2 起動目は
  「既に起動しています」を表示して終了する。

## 7.5 鍵別使用レポート（PDF）
- 管理メニュー →「鍵別レポート」で **鍵を1つ選択** し、**対象期間**（開始日〜終了日）を指定。
- 出力内容: 鍵の管理番号・名称、期間、そして貸出記録を表形式で列挙。
  | 利用者 | 貸出日時 | 返却日時 | 利用時間 |
  |---|---|---|---|
  - 未返却の記録は返却日時を「（貸出中）」と表示。
- reportlab で A4 縦の PDF を生成し、`exports/` に保存 →（任意で）既定ビューアで開く。
- 日本語は reportlab 同梱の CID フォント `HeiseiKakuGo-W5` を使用（外部フォント不要）。

## 7.6 点検記録
- 管理メニュー →「点検記録」→「点検を実施」で、点検者名・結果（問題なし/問題あり）・所見を入力し保存。
- 点検時の補助として、その時点の **貸出中一覧**（＝手元に無いはずの鍵）を画面表示し、
  実物突合の参考にできる。この一覧は `inspections.open_snapshot`（JSON）に保存し、
  後から「点検時点で何が未返却だったか」を再現できるようにする。
- 過去の点検履歴（日時・点検者・結果・所見）を一覧表示。CSV/PDF 出力は将来拡張。

## 8. スコープ外（今回は作らない）— 指摘と提案
以下は「要否を判断した結果、初版では省く」もの。必要なら言ってください。

| 機能 | 判断 | 理由 |
|---|---|---|
| 返却期限・期限切れアラート | **省略** | 社内鍵の常時貸借では過剰。必要なら後から列追加で対応可 |
| 鍵の予約機能 | **省略** | 運用が複雑化。当日その場の貸借が主目的 |
| 複数拠点・ネット共有 | **対象外** | 単一 PC 運用。将来必要なら PostgreSQL へ移行 |
| 操作者ごとのアカウント/ログイン | **省略** | 管理者 PIN で十分（ヒアリング結果） |
| 鍵の写真・地図 | **省略** | `note` 欄で代替。要望あれば追加 |

## 9. 含める機能（重要なので明記）
- **貸出中一覧**（返し忘れ把握）: 実運用で最重要。必ず入れる。
- **CSV エクスポート**: 監査・棚卸しで有用。軽量なので入れる。文字コードは
  **UTF-8 (BOM 付き)**（Excel で日本語が文字化けしないように）。
- **論理削除**: 退職者・廃棄鍵を消しても履歴が残る。必須。
- **二重貸出防止**: 同じ鍵を同時に2人へ貸さない。DB 部分ユニークインデックス＋アプリ検査の二重化。
- **DB バックアップ**: 稼働中の生コピーは不整合になりうるため、**SQLite オンライン
  バックアップ API（`Connection.backup()`）**でタイムスタンプ付きファイルを `exports/` 等へ
  出力する「バックアップ」ボタンを管理メニューに用意。加えて WAL 有効化で破損耐性を上げる。
  手動ファイルコピーはアプリ終了中に限る旨を README に明記。

## 10. ディレクトリ構成
```
Keylog/
├─ README.md              # 概要・起動方法・バックアップ手順
├─ SPEC.md                # 本仕様書
├─ pyproject.toml         # プロジェクト定義・依存（uv 管理）
├─ uv.lock                # 依存ロックファイル（uv 生成）
├─ run.py                 # 起動エントリポイント。sys.path に src を追加して keylog を import
├─ .gitignore
├─ .venv/                 # 仮想環境（uv 生成・.gitignore 対象）
├─ data/
│  └─ keylog.db           # SQLite 本体（.gitignore 対象）
├─ exports/               # PDF / CSV の出力先（.gitignore 対象）
├─ src/keylog/            # アプリ本体パッケージ
│  ├─ __init__.py
│  ├─ config.py           # パス・定数（PIN 初期値など）
│  ├─ db.py               # 接続・スキーマ作成・初期化
│  ├─ models.py           # DB アクセス（users/keys/checkouts/inspections の CRUD）
│  ├─ services.py         # 業務ロジック（貸出・返却・登録・点検・二重貸出防止）
│  ├─ reader.py           # RC-S300 ポーリング（pyscard・別スレッド）
│  ├─ reports.py          # PDF 生成（reportlab）・CSV エクスポート
│  └─ ui/                 # CustomTkinter 画面
│     ├─ __init__.py
│     ├─ app.py           # メインウィンドウ・画面遷移
│     ├─ main_view.py     # 貸出/返却
│     ├─ admin_view.py    # 管理メニュー（PIN）
│     └─ dialogs.py       # カード登録・PIN 入力などのダイアログ
└─ tools/
   └─ read_test.py        # 読み取り確認スクリプト（開発用）
```

## 10.5 セットアップ・起動（uv）
```bash
uv sync                       # .venv 作成 + 依存インストール
uv run python run.py          # アプリ起動
uv run python tools/read_test.py   # カード読み取り確認
uv add <package>              # 依存追加（pyproject.toml と uv.lock を更新）
```
- 依存の真実は `pyproject.toml` + `uv.lock`。`pip install` は使わない。
- 配布時は `uv sync` だけで同一環境を再現できる。

## 11. 決定事項（ヒアリング済み）
- [x] 鍵の初期データ: **手入力**（鍵管理画面から登録）。
- [x] GUI の見た目: **CustomTkinter**（今風）。
- [x] 未登録カードのその場登録: **管理者 PIN 必須**（登録操作は常に PIN、方針を一貫）。
- [x] 管理者 PIN 初期値: **`pass`**。
  - DB には **ソルト付き SHA-256 ハッシュ**で保存（`settings` に salt と hash）。平文・無塩にしない。
  - 初回起動時に初期 PIN を投入。管理メニューの「PIN 変更」で変更可能（初期 PIN からの変更を促す）。
  - 初期 PIN はソースにハードコードせず、初回起動処理でのみ使用する定数として扱う。

## 12. 実装前チェックリスト（fresh-eye レビュー反映済み）
- [x] 二重貸出: 部分ユニークインデックス＋`PRAGMA foreign_keys=ON`（§5）
- [x] 単一起動ガード、スレッド安全（queue + `after()`）（§7.1/§7.2）
- [x] 代理返却・記録訂正（§6.2）／貸出中の鍵・利用者の無効化ブロック（§5）
- [x] 退職者カード拒否・返却モードでの登録抑止・貸出確認ダイアログ（§6.1）
- [x] PIN ソルト付きハッシュ・変更 UI（§11）／日時ローカル naive 統一（§5）
- [x] バックアップは backup API＋WAL（§9）／CSV は UTF-8 BOM 付き（§9）
